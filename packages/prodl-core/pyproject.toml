[project]
name = "prodl-core"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "yt-dlp>=2024.1.1",
    "httpx>=0.27.0",
    "pydantic>=2.0",
    "aiofiles>=23.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
