[project]
name = "prodl-cli"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "prodl-core",
    "typer>=0.12.0",
    "rich>=13.0",
]

[project.scripts]
prodl = "prodl_cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
