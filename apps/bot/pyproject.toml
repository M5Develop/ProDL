[project]
name = "prodl-bot"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "prodl-core",
    "python-telegram-bot>=21.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
