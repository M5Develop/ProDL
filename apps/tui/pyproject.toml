[project]
name = "prodl-tui"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "prodl-core",
    "textual>=0.70.0",
]

[project.scripts]
prodl-tui = "prodl_tui:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
